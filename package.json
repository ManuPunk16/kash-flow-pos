{
  "name": "kashflow-pos",
  "version": "1.0.0",
  "description": "Sistema POS para tienda pequeña",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start -w frontend\" \"npm run dev -w api\"",
    "build": "npm run build --prefix ./frontend",
    "build:all": "npm run build --prefix ./frontend && npm run build --prefix ./api",
    "start": "npm run start --prefix ./api",
    "lint": "npm run lint --prefix ./frontend && npm run lint --prefix ./api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
