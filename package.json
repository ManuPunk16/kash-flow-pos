{
  "name": "kashflow-pos",
  "version": "1.0.0",
  "description": "Sistema POS para tienda pequeña",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w api\"",
    "build:all": "npm run build -w frontend && npm run build -w api",
    "start": "npm run start -w api",
    "lint": "npm run lint -w frontend && npm run lint -w api",
    "test": "npm run test -w frontend && npm run test -w api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
